# Data — embeddings are large and reproducible via bio_embeddings.
data
data_files/*.h5
data_files/*.h5py
data_files/*.hdf5
*.h5
*.h5py
*.hdf5

# Training artifacts — regenerated from configs.
runs
sweeps
# t-SNE feature dumps (large .npy scratch; only the PNG in results/ is kept)
features/

# Python
__pycache__
*.py[cod]
*.egg-info/
bio
bio_embeddings_env
.venv/
venv/
env/
build/
dist/

# IDE / tooling
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Old project conventions
bsub.yaml
bsub.yml
bsub*
stderr.log
stdout.log
temp
trained_model_weights
embedder_model
data_files/bio_embeddings_runs/
data_files/flip_meltome/
data_files/*_remapped.fasta
data_files/*.h5

# Misc artifacts
*.npy
*.csv
*.png
*.jpg
*.log
.ipynb_checkpoints/

# --- Submission artifacts: tracked despite the broad ignores above ---
# Frozen unsupervised projection checkpoints (tiny, ~270 KB each).
# The heads/ subtree stays ignored (see above) and lives on Google Drive.
!checkpoints/
!checkpoints/**
# Final results table, workbook, significance output, and figures
!results/
!results/**
